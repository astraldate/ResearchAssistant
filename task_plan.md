# ResearchAssistant 工作计划

## 目标

在保留既有审计成果的基础上，实现移动端 PDF AI 自然选字、整页翻译、术语解释与知识卡片保存；实现带本地论文引用、移动创新点卡片和桌面 Idea Map 支持边的 A+B 创新分析；为移动会话增加 `/` 指令与 `@` 论文选择，并整理演示论文集。

## 当前阶段

阶段 36：移动端段落边界与跨样式选区修复已完成

## 阶段

### 阶段 1：范围与基线确认

- [x] 确认用户需要项目审计报告
- [x] 识别仓库顶层结构、技术栈和已有文档
- [x] 记录审计范围与限制
- **状态：** 完成

### 阶段 2：代码与配置审查

- [x] 审查桌面端、移动端、Rust 后端和共享协议的组织方式
- [x] 审查构建、测试、持续集成与发布配置
- [x] 审查错误处理、日志、依赖与安全相关配置
- **状态：** 完成

### 阶段 3：验证与风险核验

- [x] 执行现有静态检查与构建前置校验
- [x] 对关键风险进行代码级交叉核验
- [x] 按影响和可能性确定优先级
- **状态：** 完成

### 阶段 4：报告编写与复核

- [x] 编写中文审计报告
- [x] 复核报告中的证据定位与结论边界
- [x] 记录未覆盖事项和后续建议
- **状态：** 完成

### 阶段 5：共享协议与后端服务

- [x] 扩展移动协议、会话引用与创新分析类型
- [x] 抽取可复用 PDF AI 服务并修正模型与降级逻辑
- [x] 增加受认证的 PDF AI、PDF.js 与创新意图路由
- [x] 实现双路本地论文检索、引用约束与持久化
- **状态：** 完成

### 阶段 6：移动端交互

- [x] 实现创新概念确认、引用卡片与论文页跳转
- [x] 实现在线 PDF.js 原位划词和离线原生阅读回退
- [x] 实现翻译、解释、整页翻译、缓存和保存卡片
- **状态：** 完成

### 阶段 7：测试、构建与复核

- [x] 增加关键协议与检索行为测试
- [x] 通过 Web、移动端与 Rust 静态检查和测试
- [x] 构建 Android debug APK，并在签名材料允许时验证 release
- [x] 检查兼容性、错误提示与最终改动范围
- **状态：** 完成

### 阶段 8：自然选字、创新图谱与演示交付

- [x] 将矩形框选替换为视觉阅读流线性选字、直接拖动和端点手柄
- [x] 将有本地证据的 A+B 回答持久化为 Idea，并生成 Paper → Idea 支持边
- [x] 在手机回答中显示 A × B、证据覆盖和 2–3 条创新点
- [x] 增加移动 `/` 指令选择器和结构化 `@` 论文上下文
- [x] 保留 6 篇演示论文并按用户授权永久删除其余 114 篇
- [x] 构建并核验桌面 1.1.8 NSIS 与 Android 1.1.8 release APK
- **状态：** 完成

### 阶段 9：移动端导航与资料库整合

- [x] 将聊天页改为“聊天 / 采集”顶部页内切换，并从底部 Tab 隐藏独立采集入口
- [x] 将卡片库改为“卡片 / 笔记 / 复习”顶部页内切换，并从底部 Tab 隐藏独立复习入口
- [x] 核对并接入桌面笔记库与知识卡片同步数据，补充删除接口和删除确认
- [x] 保持聊天输入 Dock 固定底部，确保 `/` 与 `@` 菜单在其上方独立滚动
- [x] 更新中文文档、统一 1.1.9 版本并通过桌面与 Android release 构建
- **状态：** 完成

### 阶段 10：移动端输入、解释编码与创新展示修复

- [x] 让 `/`、`@` 按钮与候选选择真实写入输入栏，同时保留结构化 command 与 paperContext
- [x] 修复 PDF 页面上下文中的替换字符、错误日期与 DOI 乱码，不把低质量片段展示或传给模型
- [x] 调整移动设置页声明和版本信息
- [x] 增加明确的创新生成入口与创新结果浏览视图
- [x] 为聊天回答、创新分析、卡片与笔记增加安全的移动 Markdown 渲染
- [x] 更新 README、Design Specification 和相关计划文档，并构建 1.1.10 桌面与 Android 产物
- **状态：** 完成

### 阶段 11：移动编辑能力与创新删除

- [x] 让 `/` 与 `@` 在当前光标或选区处插入，并修复选择快捷项后的发送状态
- [x] 增加知识卡片创建与编辑的受控移动协议、桌面路由和移动表单
- [x] 增加论文笔记创建与编辑的受控移动协议、桌面路由和移动表单
- [x] 增加创新结果与桌面 Idea Map 的一致删除能力
- [x] 清洗或隐藏引用证据中的乱码片段
- [x] 更新文档、测试、版本与桌面/Android 正式产物
- **状态：** 完成

### 阶段 12：快捷命令与 PDF AI 降级一致性修复

- [x] 允许 `/brief @论文` 等自带明确语义的快捷组合直接发送，并保持需要参数的命令校验
- [x] 修复桌面划词翻译遇到损坏 ToUnicode CMap 时直接失败的问题
- [x] 核对桌面与移动术语解释的共享服务，保证模型总结始终存在、百科作为可选增强、页面 Context 只作为证据
- [x] 增加命令默认问题与 PDF 提取降级测试
- [x] 更新文档、版本并重建桌面端与 Android 正式包
- [ ] 将 1.1.12 覆盖到受保护分发目录并静默升级已安装桌面端（环境授权额度耗尽，等待手动安装）
- **状态：** 等待手动安装

### 阶段 13：发布文档、CI/CD 与 Git 标签交付

- [x] 审查现有 CI、桌面发布和 Android 发布工作流
- [x] 增加版本一致性、格式、测试与构建门禁并优化并发和缓存
- [x] 更新 README、Design Specification 与发布说明
- [x] 完成本地 MVP 快速门禁并复核待提交差异
- [x] 按关注点提交改动，创建 `v1.1.12` 标签并推送 `main` 与标签
- **状态：** 完成

### 阶段 14：Windows CI 与比赛发布稳定性修复

- [x] 消除 Windows CRLF 导致的 Prettier 全仓误报
- [x] 移除 Windows 与 Android 发布任务的 90 分钟限制
- [x] 用 GitHub CLI 重试替代 Tauri/softprops 的 Release 创建与上传路径
- [x] 增加指定 tag 的桌面发布手动重跑入口
- [x] 完成快速语法与格式验证，更新文档并推送修复
- **状态：** 完成

### 阶段 15：Rust 应用级 OCR 与长期稳定化

- [x] 收束并验证移动聊天中断、异常退出状态恢复与现有未提交改动
- [x] 建立独立 Rust OCR 模块、模型运行库清单、任务持久化与 Sidecar
- [x] 实现 PDF 诊断与版面阅读顺序，验证乱码页、双栏和图表隔离
- [x] 实现可恢复 OCR 作业执行器、独立页面缓存、失败页重试与旋转坐标转换
- [ ] 接入真实 PP-OCRv6/PP-DocLayout ONNX 推理
- [x] 按用户要求移除 OCR SHA-256 门禁，改用 HTTPS 清单、非空文件和可选大小检查
- [ ] 接入桌面 OCR 管理界面、移动单页 Sidecar 接口、区域化选字与 Research Memory 写入
- [x] 完成移动端方向解锁、统一响应式断点、平板导航和 PDF 横屏阅读布局
- [x] 完成平板论文多列网格
- [x] 升级至 1.1.13 并构建 Windows NSIS 与 Android release
- [ ] 完成聊天、知识库和编辑器的平板主从布局
- [ ] 实现密码加密便携备份、恢复校验与系统健康检查
- [x] 增加无模型测试、更新文档并通过桌面与移动构建门禁
- **状态：** 进行中

### 阶段 16：移动端 PDF 沉浸式阅读升级

- [x] 将在线 PDF.js 与离线原生阅读器统一为默认连续滚动，并保留单页模式
- [x] 建立全屏阅读壳层、紧凑工具栏、目录抽屉和非模态译文栏
- [x] 升级 PDF Viewer v2 消息协议、邻页渲染窗口和逐页选区状态
- [x] 迁移桌面划词翻译模式，完成自动翻译、缓存和最新选区优先
- [x] 增加协议与纯逻辑测试，通过双端、Rust 和冲突标记门禁
- [x] 统一升级至 1.1.14、Android versionCode 24，并构建 Windows 与 Android 正式产物
- **状态：** 完成

### 阶段 17：移动端 PDF 选区稳定性修复

- [x] 复现并定位选区突然拓展的 Range、端点或跨页状态原因
- [x] 修复文字层选区边界，阻止失效坐标、跨页和异步回写扩大选区
- [x] 增加选区稳定性纯逻辑与 Viewer 回归测试
- [x] 通过移动 TypeScript、Rust 定点测试、格式和冲突标记检查
- [x] 重建 Android Release APK，核验版本与签名并更新真机分发文件
- **状态：** 完成

### 阶段 18：移动端 PDF 真机选区回归修复

- [x] 从视觉行重新聚类阅读栏，拆开已经混入左右栏的污染阅读流
- [x] 过滤空白字符命中，只允许触摸可见字形直接选字并确保滑动手势优先
- [x] 增加污染流拆栏、空白命中和滚动取消回归测试
- [x] 通过 Rust、移动 TypeScript、Viewer 语法、格式与冲突标记门禁
- [x] 重建 Android Release APK，核验版本与签名并更新真机分发文件
- [x] 重建 Windows NSIS，确保桌面移动服务实际提供新 Viewer
- **状态：** 完成

### 阶段 19：移动端 Viewer 修订固化与 APK 真实重建

- [x] 在移动端 URL 与 WebView 生命周期中固化 Viewer 修订号并强制新会话加载
- [x] 增加 URL 修订、协议握手和重新加载回归测试
- [x] 通过移动 TypeScript、PDF 阅读器测试、格式和冲突标记门禁
- [x] 真实重新生成 Android Release APK，核验新构建时间、版本和签名
- **状态：** 完成

### 阶段 20：双栏污染行拆分与跨行选区修复

- [x] 在栏聚类前按字符栏沟拆分横跨双栏的污染视觉行
- [x] 使用视觉 `lineId` 恢复同栏跨行文本与选区连续性
- [x] 增加单个污染行拆栏、同栏下一行连续和右栏隔离回归测试
- [x] 通过 Viewer 语法、Rust、移动 TypeScript、格式和冲突标记门禁
- [x] 重建并核验配套 Windows NSIS 与 Android Release APK
- **状态：** 完成

### 阶段 21：空白轻点取消选区闭环

- [x] 将空白轻点识别为延迟候选，抬起时清除 Viewer 选区
- [x] 保持空白滑动滚页优先，移动超过阈值不得清除当前选区
- [x] 让移动宿主收到空选区消息后清除当前选中文字状态
- [x] 增加 Viewer 与移动宿主回归测试并通过完整质量门禁
- [x] 重新构建并核验 Android Release APK 与 Windows NSIS
- **状态：** 完成

### 阶段 22：第七篇论文移动同步诊断与修复

- [x] 核对桌面论文数据、移动论文列表接口和移动端分页/缓存链路
- [x] 定位最新论文未出现的确定性原因并实施最小修复
- [x] 增加同步回归测试并通过相关质量门禁
- [x] 与阶段 21 一并重新构建、核验并更新正式产物
- **状态：** 完成

### 阶段 23：Tencent 2.0 模型中心兼容性与发布

- [x] 核对用户当前未提交改动与模型中心的 Provider、推荐模型、镜像下载和已安装模型识别
- [x] 确认 Tencent 2.0 的实际 Ollama/ GGUF 模型标识、下载地址和运行时兼容性
- [x] 在确认兼容后更新默认模型、模型中心推荐项、镜像映射和旧配置迁移
- [x] 增加模型解析与迁移回归测试，更新版本和中文发布记录
- [x] 通过 Rust、Web、移动 TypeScript、格式和冲突检查
- [x] 构建并核验 Windows NSIS 与 Android Release APK
- **状态：** 完成

### 阶段 24：国内镜像下载回归修复

- [x] 移除不存在的 Tencent ModelScope 候选和国内不可用的 Hugging Face 官方直连
- [x] 验证 `hf-mirror.com` 带 `download=true` 的 HY-MT2 GGUF 地址
- [x] 改进多候选下载错误汇总并重建 Windows NSIS
- **状态：** 完成

### 阶段 25：Ollama GGUF 导入与应用闭环

- [x] 读取 Ollama 400 响应正文并确认旧 `modelfile` API 已失效
- [x] 改用 `ollama create -f Modelfile` 导入本地 GGUF，并验证模型列表
- [x] 运行短翻译验证，确认 HY-MT2 可正常推理
- [x] 增加导入后模型名称校验与回归测试
- [x] 重建并核验 Windows NSIS
- **状态：** 完成

### 阶段 26：模型实时热榜与部署预算筛选

- [x] 将模型元数据查询切换到国内 `hf-mirror.com` API，并支持关键词检索
- [x] 读取 GGUF 文件大小，按下载热度排序并按 4–32GB 部署预算筛选
- [x] 在模型中心区分“内置推荐”和“实时热榜”，显示大小、部署估算与预算标记
- [x] 增加文件选择、部署估算、预算过滤和热度排序回归测试
- [x] 通过 Rust、Web、格式和冲突检查，并重建 Windows 1.1.15 NSIS
- **状态：** 完成

## 关键问题

1. 当前项目的主要质量、可维护性和安全风险位于哪些模块？
2. 现有自动化检查是否能及时发现这些风险？
3. 哪些改进应优先投入？

## 已作决定

| 决定                                           | 理由                                                                                |
| ---------------------------------------------- | ----------------------------------------------------------------------------------- |
| 审计后进入实现                                 | 用户随后明确要求按实施方案修改产品代码并构建交付物                                  |
| 覆盖静态代码、配置与本地验证                   | 平衡证据充分性与审计成本                                                            |
| Android 采用在线 PDF.js 与离线原生阅读混合模式 | 满足原位划词并保留离线能力                                                          |
| A+B 仅检索本地 Research Memory                 | 遵循用户确认的本地优先与不伪造引用要求                                              |
| 无论文时允许模型继续但不生成引用               | 保证可用性并明确证据边界                                                            |
| 移动端同步全部当前工作区论文                   | 比赛演示已结束，用户明确授权退役 6 篇白名单并回归实用路线                           |
| 移动创新点卡片由持久化正文派生                 | 避免增加与完整回答可能不一致的第二份模型摘要                                        |
| 底部导航只保留高频主域                         | 采集属于聊天输入动作，复习属于卡片生命周期，分别放入对应页面顶部切换可减少 Tab 拥挤 |
| 1.1.12 发布采用 MVP 快速门禁                   | 用户明确要求避免耗时环节；复用刚完成的正式构建结果，本轮只跑快速静态检查与轻量测试  |
| Android 只交付比赛演示包                       | 用户明确无需上架准备；移除正式签名分支，产物固定标记为 `-demo.apk`                  |
| OCR 改为 Rust 应用级 Sidecar                   | 避免 Python 环境、隐藏字体与 ToUnicode 导致的选区漂移，保持原 PDF 不变              |
| 图表文字隔离但可选                             | 允许用户解释图中文字，同时防止图表噪声污染正文检索与 A+B 证据                       |
| OCR 仅处理诊断问题页                           | 保留正确的电子文字层，降低长论文耗时并减少错误覆盖                                  |
| OCR 模型首次使用下载                           | 控制安装包体积；按用户要求取消 SHA-256，保留 HTTPS 清单、非空文件与可选大小检查     |
| 便携备份必须密码加密                           | 论文、会话、笔记与 Idea 属于科研隐私数据，不提供易误用的明文导出                    |
| 移动 PDF 默认连续滚动                          | 用户需要更大的有效阅读窗口，并希望迁移桌面端连续页面阅读体验                        |
| 划词翻译采用会话级自动模式                     | 开启一次后每个稳定选区自动翻译，不把“连续翻译”误实现为滚动触发整页翻译              |
| 目录覆盖显示、译文按设备停靠                   | 目录默认收起避免占宽；平板译文右置、手机译文底置，兼顾对照阅读与文档面积            |
| v2 PDF 统一使用受控视觉选区                    | 避免 Android 原生 Range 按 PDF DOM 顺序向前或向后扩展，同时保留应用内复制和 AI 操作 |

## 错误记录

| 错误                                                             | 次数 | 处理方式                                                               |
| ---------------------------------------------------------------- | ---: | ---------------------------------------------------------------------- |
| Markdown 样式常量在定义前使用导致移动 TypeScript 失败            |    1 | 改用受 React Native 支持的固定 `monospace` 字体名后通过检查            |
| 沙箱身份无法写入 `mobile-app/dist/android` 分发目录              |    3 | 核验目录和源 APK 后按权限流程执行受控复制，稳定文件已更新为 1.1.11     |
| 新共享笔记 helper 误继承 `#[tauri::command]`，产生重复命令宏     |    1 | 将属性移回真正的异步 command 代理函数                                  |
| 新增 Rust 测试字符串不符合 rustfmt 换行                          |    1 | 执行项目标准 `cargo fmt` 后重新检查                                    |
| planning-with-files 完成检查器无法识别中文“阶段”标题，报告 0/0   |    1 | 以任务清单全部勾选和完整验证表作为完成依据，不改成英文破坏仓库中文规范 |
| PowerShell 中组合正则的引号转义导致 `rg` 搜索失败                |    2 | 拆分为不含嵌套引号的简单模式搜索                                       |
| GitHub API 引用地址被浏览安全策略拒绝                            |    1 | 改用 Git 读取官方仓库公开引用，不手工猜测 Action 提交哈希              |
| 沙箱内 Git 无法连接 GitHub 公开仓库                              |    1 | 按网络权限流程执行只读 `git ls-remote`，成功取得官方 Action 引用       |
| 合并补丁因 Markdown 表格空格上下文未命中                         |    1 | 拆分为工作流与计划文件小补丁，并使用准确行内容完成修改                 |
| Git 暂存首次因 `.git/index.lock` 权限被拒绝                      |    1 | 按权限流程重试 `git add` 后成功，未绕过仓库保护                        |
| 创建 `v1.1.12` 标签的自动权限审核超时                            |    1 | 按系统允许仅重试一次，第二次成功创建标签                               |
| CI Rust 任务缺少 PDF.js 打包资源                                 |    1 | 在 Rust 检查前以 `--ignore-scripts` 恢复 pnpm 资源树                   |
| CI 热修跨文件补丁在后段上下文未命中                              |    2 | 复核确认前段工作流和文档已写入，仅对未写入的计划记录使用精确小补丁     |
| 普通 CI 将分支名 `main` 误当成发布标签                           |    1 | 仅在显式 `--tag` 或 `GITHUB_REF_TYPE=tag` 时执行标签一致性校验         |
| CI 热修推送命令等待 pre-push hooks 超时                          |    1 | 先核对远端提交状态，再以更长超时或跳过已验证的重复 hook 完成推送       |
| 本机未安装 GitHub CLI，无法读取 Actions 实时状态                 |    1 | 保留已确认的 Git 推送证据，不安装额外工具或阻塞 MVP 交付               |
| CI 修复补丁中的已修正 PowerShell 行未再次命中                    |    1 | 复核确认目标行已是正确写法，改为只更新尚未应用的文档部分               |
| 完整 Prettier 检查仍发现 5 个真实格式差异                        |    1 | 对 5 个文件执行机械格式化，复检完整门禁通过                            |
| OCR 跨文件补丁首次被 Windows 沙箱助手取消                        |    1 | 核对无文件落盘后拆为模块入口与 `lib.rs` 两个小补丁                     |
| 断线竞态补丁因格式化后的 Rust 上下文未命中                       |    1 | 复核确认 Rust 部分已经写入，仅对未应用的移动端片段使用精确小补丁       |
| OCR 跨栏标题测试夹具使用普通正文标签导致断言失败                 |    1 | 将跨栏区域显式标记为 `document_title`，保留真实标题识别语义            |
| 最终差异审查未引用含 `(tabs)` 的 PowerShell 路径                 |    1 | 改用带双引号的字面路径重新审查，不影响已完成的质量门禁                 |
| 响应式 Node 测试在受限环境中派生进程时报 `spawn EPERM`           |    1 | 按权限流程在受控环境重跑，不把环境限制误判为代码失败                   |
| 响应式测试直接导入 React Native 时 Node 无法解析 Flow 语法       |    1 | 抽取无平台依赖的断点纯函数模块，Hook 仅负责读取窗口尺寸                |
| Rust 全库测试首次冷编译超过 120 秒工具上限                       |    1 | 保留已完成编译缓存，以更长但有限的超时继续执行                         |
| OCR 作业测试夹具未先保存资产而触发 SQLite 外键约束               |    1 | 在创建任务前保存测试资产并移除无用导入，生产状态机无需修改             |
| 新增坐标测试存在两处 `rustfmt` 自动换行差异                      |    1 | 执行标准 `cargo fmt` 后继续完整门禁                                    |
| OCR 模块入口重复声明 `coordinates` 导致编译失败                  |    1 | 删除重复模块声明并重新执行 Rust 全量门禁                               |
| 全仓 Prettier 检查发现 7 个生成文件或计划文件格式差异            |    1 | 对报告文件执行机械格式化，再复检全仓                                   |
| 仓库不存在预期的 `scripts/set-version.mjs`                       |    1 | 使用现有版本一致性脚本列出的字段做受控小补丁升级                       |
| Cargo 版本更新后 4 个 Tauri 生成 Schema 出现格式差异             |    1 | 仅对生成 Schema 执行机械 Prettier 格式化                               |
| 首次复制 1.1.13 APK 到受保护分发目录被 ACL 拒绝                  |    1 | 按权限流程受控更新版本化文件与稳定文件                                 |
| 移动 PDF 阅读页整文件补丁使用绝对路径删除失败                    |    1 | 改用工作区相对路径执行删除与新增，已确认原文件未发生变化               |
| 新建移动 screens 目录被工作区 ACL 拒绝                           |    1 | 将新阅读页放入已有 components 目录，不申请额外权限                     |
| PDF 阅读器 Node 测试在受限环境派生进程时报 spawn EPERM           |    2 | 按既有方式在受控环境重跑单项测试，其他门禁改为分别执行                 |
| PDF 阅读器定点 Prettier 检查发现 4 个机械格式差异                |    1 | 仅格式化新增源码与计划文件，随后分别重跑 Rust 门禁                     |
| 统一版本跨文件补丁因两处字段已更新而中段停止                     |    1 | 核对全部版本字段，只补仍为 1.1.13 的文件并单独更新文档                 |
| 阶段 18 Viewer 回归断言不符合 rustfmt 换行                       |    1 | 执行项目标准 `cargo fmt` 后分别重跑门禁                                |
| 删除重复 CSS 后 Rust 断言仍要求规则立即闭合                      |    1 | 改为检查 `touch-action` 规则前缀，避免把后续合法属性误判为失败         |
| 移动 PDF Node 测试在沙箱再次出现 spawn EPERM                     |    1 | 按既有权限流程在受控环境重跑                                           |
| 阶段 18 三份中文计划文件出现 Prettier 机械差异                   |    1 | 仅格式化 `task_plan.md`、`findings.md` 和 `progress.md`                |
| 冲突标记脚本在沙箱调用 git 再次出现 spawnSync EPERM              |    1 | 按既有权限流程在受控环境重跑                                           |
| Windows NSIS 构建在沙箱启动 Esbuild 时出现 spawn EPERM           |    1 | 按既有权限流程在受控环境重跑正式构建                                   |
| 阶段 18 覆盖两份 APK 分发文件被 ACL 拒绝                         |    1 | 核验源和目标后按权限流程受控覆盖两个明确文件                           |
| 检索移动 URL 定义时工具编排脚本缺少工作目录并产生语法错误        |    1 | 改为完整工具调用参数后重新执行只读检索                                 |
| Windows 下 `rg` 测试文件通配符被当作非法路径                     |    1 | 改为对目录检索并用 `-g` 指定文件模式                                   |
| 最终 Prettier 命令误包含 Rust 文件而无法推断解析器               |    1 | 排除 `.rs` 后重跑 Prettier，Rust 继续使用 `cargo fmt --check`          |
| 阶段 21 组合检索命令的 PowerShell 引号被截断                     |    1 | 改用多个固定字符串分别检索，避免组合正则与嵌套引号                     |
| 阶段 21 跨文件补丁的移动端片段缺少闭合上下文                     |    1 | 确认未写入后拆分为 Rust Viewer、移动宿主和纯逻辑测试三个补丁           |
| 阶段 22 并行检索包含不存在的 `mobile-app/src/api` 目录           |    1 | 使用已定位的 `src/lib/api.ts`、论文页面与 Rust 路由继续定点检查        |
| 阶段 22 验证已删除符号时 `rg` 无匹配返回退出码 1                 |    1 | 后续无匹配预期检索显式容忍退出码，只把实际输出作为验证结果             |
| 阶段 23 沙箱网络访问 Hugging Face/ModelScope 被拒绝              |    1 | 按网络权限流程重试只读模型注册查询，不改变项目或下载模型               |
| 阶段 23 网络查询并行编排脚本括号错误                             |    1 | 确认没有命令执行，改用正确的 `Promise.allSettled` 调用结构             |
| 阶段 23 PowerShell HEAD 检查使用非法管道语法                     |    1 | 改为先累积结果数组再格式化，保持只读网络检查                           |
| 阶段 22 全量同步补丁首次被数据暴露风险门禁拒绝                   |    1 | 恢复白名单后向用户说明风险；用户明确比赛结束并授权同步全部工作区       |
| 阶段 22 移动 PDF Node 测试在沙箱派生进程时报 `spawn EPERM`       |    1 | 按既有受控终端方式重跑该只读测试，其他门禁分别回收                     |
| 阶段 22 全仓 Prettier 发现计划文件机械格式差异                   |    1 | 仅对 `task_plan.md` 执行仓库标准格式化后复检                           |
| 阶段 22 冲突标记脚本在沙箱调用 Git 报 `spawnSync EPERM`          |    1 | 按既有受控终端方式重跑只读冲突检查                                     |
| 双端构建编排脚本缺少数组语句结尾                                 |    1 | 确认命令未执行，补充分号后重新启动两个明确构建命令                     |
| NSIS 子进程路径含空格但未使用 PowerShell 调用运算符              |    1 | Android 已成功；用 `&` 调用已核验的 PowerShell 路径单独重跑 NSIS       |
| 阶段 22 覆盖两份 APK 分发文件被 ACL 拒绝                         |    1 | 源与两个明确目标均已核验位于工作区，按权限流程受控覆盖                 |
| 选区稳定性测试新增长断言不符合 rustfmt 换行                      |    1 | 执行项目标准 `cargo fmt`，不改动业务逻辑                               |
| 组合检索 selectionMode 的 PowerShell 正则引号被截断              |    1 | 拆成多个固定字符串检索，不重复使用嵌套组合正则                         |
| 原生选区跨四文件补丁因内嵌 CSS 长行上下文未命中                  |    1 | 拆分移动端逻辑与 Rust CSS 小补丁，避免长行阻断其他文件                 |
| 阶段 16 收尾补丁引用了 findings.md 中不存在的进度行              |    1 | 改用该文件实际末尾内容拆分补丁，避免跨文件补丁整体回滚                 |
| 复制 1.1.14 APK 到受保护分发目录被 ACL 拒绝                      |    2 | 核验目标路径后按权限流程受控重试，版本化文件与稳定文件均更新成功       |
| 1.1.14 Cargo 版本更新后 4 个 Tauri Schema 出现格式差异           |    1 | 仅对生成 Schema 执行机械 Prettier 格式化并复检全仓                     |
| 冲突标记脚本在沙箱调用 git 时出现 spawnSync EPERM                |    1 | 在受控环境重跑只读冲突检查，版本与差异检查改为分别执行                 |
| Web 生产构建在沙箱启动 Esbuild 时出现 spawn EPERM                |    1 | 在受控环境重跑既有 `pnpm build`，不修改源码或依赖                      |
| 统一版本跨文件补丁因两处字段已更新而中段停止                     |    1 | 核对全部版本字段，只补仍为 1.1.13 的文件并单独更新文档                 |
| 阶段 25 PowerShell 循环结果直接接管道导致解析失败                |    1 | 先累积结果数组，再统一输出表格                                         |
| 阶段 25 初次编译缺少桌面命令隐藏窗口 helper                      |    1 | 在 `lib.rs` 增加平台化 helper，并移除已废弃导入后通过编译              |
| 阶段 26 文件树探测命令直接将循环接入管道导致 PowerShell 解析失败 |    1 | 先收集结果数组再转换 JSON，随后成功核验三个国内 API 地址               |
| 阶段 26 端到端探测首次未输出预算内模型                           |    1 | 修正 PowerShell revision 变量边界，并补充点号量化文件名优先级识别      |
| 阶段 26 全仓 Prettier 发现锁文件与 4 个 Tauri Schema 差异        |    1 | 仅对这 5 个机械生成文件执行标准格式化                                  |
| 阶段 26 冲突检查在沙箱派生 Git 时出现 `spawnSync EPERM`          |    1 | 在受控终端重跑同一只读脚本并通过                                       |

### 阶段 27：短段落翻译回归修复与 Windows 打包

- [x] 修复推荐模型测试夹具缺少 `updated_at` 字段的问题
- [x] 增加 HY-MT2 短段落空返回、JSON 提取和重试前纯逻辑回归测试
- [x] 通过 Rust 单元测试、Web TypeScript、Rust 格式和冲突标记检查
- [x] 构建并核验 Windows 1.1.15 NSIS 安装包
- [x] 记录构建产物位置、版本和验证结果
- **状态：** 完成

## 阶段 27 错误记录

| 错误                                           | 次数 | 处理方式                                      |
| ---------------------------------------------- | ---: | --------------------------------------------- |
| 新增 Rust 测试函数名包含空格，导致格式解析失败 |    1 | 改为无空格的 Unicode 函数标识符后重跑格式检查 |
| 统一翻译提示词改造后残留旧 `fatal` 错误映射    |    1 | 删除旧 Fatal 分支后将该路径统一改为可重试错误 |

### 阶段 28：HY-MT2 非忠实译文拦截与重新打包

- [x] 收紧 HY-MT2 直出 Prompt，明确源文本边界和禁止新增内容
- [x] 增加英文主导输出与新增摘要结构的校验
- [x] 为用户反馈样例增加纯逻辑回归测试
- [x] 通过 Rust、Web、格式和冲突检查
- [x] 重新构建并核验 Windows 1.1.15 NSIS 安装包
- **状态：** 完成

## 阶段 28 错误记录

| 错误                                                        | 次数 | 处理方式                                         |
| ----------------------------------------------------------- | ---: | ------------------------------------------------ |
| 沙箱内 Vite 启动 esbuild 时出现 `spawn EPERM`               |    1 | 在受控环境重跑正式 NSIS 构建并成功完成           |
| release 编译发现失败类型字段仅测试使用而产生 dead-code 警告 |    1 | 移除生产不需要的失败类型字段并重新测试、重新打包 |

### 阶段 29：Cloudflare Tunnel 自动启用与 Mobile 地址展示

- [x] 修复 `cloudflared` 标准输出和标准错误的公网地址捕获
- [x] 增加 Windows 常见安装路径查找，并在启动时自动建立临时 Tunnel
- [x] 在 Mobile 区显示可直接填入手机配对页的推荐地址
- [x] 自动刷新异步生成的 Cloudflare 公网地址和 Tunnel 状态
- [x] 通过 Rust、Web、移动端类型与冲突检查
- [x] 重新构建并核验 Windows 安装包
- **状态：** 完成

## 阶段 29 错误记录

| 错误                                                  | 次数 | 处理方式                               |
| ----------------------------------------------------- | ---: | -------------------------------------- |
| 标准库 `mpsc` 与 Tokio `mpsc` 同名导致 Rust 编译冲突  |    1 | 为标准库通道使用 `std_mpsc` 别名       |
| `ChildStdout` 与 `ChildStderr` 不能放入同一个异构数组 |    1 | 增加泛型输出读取器，分别消费两个管道   |
| 版本补丁中部分文件已提前更新导致上下文不匹配          |    1 | 重新读取当前字段，只补未更新的版本文件 |

### 阶段 30：翻译模型选择同步与输出清洗

- [x] 移除桌面端把已保存 HY‑MT1.5 强制改成 HY‑MT2 的迁移逻辑
- [x] 移除移动端把 HY‑MT2 强制改成 HY‑MT1.5 的迁移逻辑，原样使用桌面端当前选择
- [x] 清洗 JSON `translation` 字段和普通输出中的空 `[]`、翻译标记及包装字符
- [x] 增加空 JSON、空标记、正常引用保留和重试路径回归测试
- [x] 通过 Rust 67/67、Web 类型、版本一致性、冲突标记和差异检查
- [x] 构建并核验 Windows 1.1.17 NSIS 安装包
- **状态：** 完成

### 阶段 31：翻译标记重复输出修复

- [x] 兼容一层、两层和三层 `TRANSLATION` 标记
- [x] 截断标记前被模型回显的重复译文
- [x] 增加用户反馈的 Conclusions 长文本回归测试
- [x] 构建并核验 Windows 1.1.18 NSIS 安装包
- **状态：** 完成

### 阶段 32：移动端段尾选区与空译文重试修复

- [x] 放宽末尾字形触控命中范围，同时保留空白区域与非字形拦截
- [x] 空译文改用无页面上下文的严格原文边界 Prompt 重试
- [x] 通过 Rust 70/70、移动端 PDF 测试 7/7、移动端 TypeScript 和差异检查
- [x] 构建并核验 Android Release APK 与 Windows 1.1.18 NSIS 安装包
- **状态：** 完成

### 阶段 33：桌面端 PDF 选区视觉偏移修复与双端重打包

- [x] 让桌面端选区高亮按实际字符 Range 计算，消除高亮多出末字符的问题
- [x] 移除桌面 PDF 重绘期间的临时缩放坐标系，保证 canvas、文字层和注释比例一致
- [x] 增加桌面选区可执行静态门禁，覆盖字符边界和临时缩放残留
- [x] 通过桌面端 TypeScript、Rust 与移动端 TypeScript/PDF 测试、格式和冲突检查
- [x] 在不修改版本号 1.1.18 的前提下重新构建并核验 Windows NSIS 与 Android Release APK
- **状态：** 完成

### 阶段 34：桌面端 PDF 选区回退修复与双端重打包

- [x] 修复字符级矩形计算阻断桌面原生选区的问题
- [x] 增加原始 Range 矩形回退，确保 PDF.js 重绘或几何异常时仍可选中
- [x] 通过桌面端和移动端类型、测试、格式、版本及冲突检查
- [x] 保持版本 1.1.18，重新构建并核验 Windows NSIS 与 Android Release APK
- **状态：** 完成

### 阶段 35：移动端段首段尾选区连续性修复

- [x] 修复单栏宽正文被错误拆成多个视觉 flow 的问题
- [x] 修复触摸跨行时终点被强制钳回锚点 run 的问题
- [x] 校正段首、段尾手柄与字符边界的定位，并保留双栏隔离
- [x] 增加 Viewer 静态回归断言并通过 Rust、移动端和桌面端质量门禁
- [x] 在不修改版本号的前提下重新构建 Android Release APK 与 Windows NSIS
- **状态：** 完成

### 阶段 36：移动端段落边界与跨样式选区修复

- [x] 移除段首段尾必须直接命中文字 span 的前置限制，保留字形距离与空白滑动门禁
- [x] 让同一视觉行的小标题、粗体、斜体与后续正文进入同一阅读流
- [x] 收紧多栏识别证据，避免少量跨样式行被误判为双栏
- [x] 增加段落边界、跨样式连续性与双栏隔离回归断言
- [x] 通过 Rust、移动端、格式、版本和差异门禁并重新打包双端
- **状态：** 完成
